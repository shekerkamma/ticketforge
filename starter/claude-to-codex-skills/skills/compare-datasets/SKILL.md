---
name: compare-datasets
description: Compare metrics, schema assumptions, and findings across two or more connected datasets. Use when the user wants to know whether a pattern is shared, unique, or inconsistently defined across datasets.
---

# Compare Datasets

## Workflow

1. Identify the datasets to compare.
2. Load shared metric definitions and schema notes.
3. Compare:
   - metric definitions
   - baseline ranges
   - recurring findings
   - obvious divergences
4. Save a cross-dataset note under `.knowledge/analyses/` or the active run.
5. End with concrete recommendations:
   - align definitions
   - investigate divergence
   - segment by product or market
